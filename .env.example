# Root env file consumed by docker-compose.
# Copy to .env. Leaving GEMINI_API_KEY empty runs the stack in mock mode.

GEMINI_API_KEY=
GEMINI_CHAT_MODEL=gemini-2.0-flash
GEMINI_EMBED_MODEL=gemini-embedding-001
